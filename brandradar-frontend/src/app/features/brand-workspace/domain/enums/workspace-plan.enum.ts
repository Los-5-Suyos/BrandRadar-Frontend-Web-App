/**
 * WorkspacePlan — Value Object del dominio Brand Workspace
 * Define el nivel de suscripción del workspace.
 */
export enum WorkspacePlan {
  FREE       = 'FREE',
  PRO        = 'PRO',
  ENTERPRISE = 'ENTERPRISE',
}
