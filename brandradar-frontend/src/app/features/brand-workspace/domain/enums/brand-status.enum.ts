/**
 * BrandStatus — Value Object del dominio Brand Workspace
 * Ciclo de vida de una marca dentro del workspace.
 */
export enum BrandStatus {
  ACTIVA    = 'ACTIVA',
  PAUSADA   = 'PAUSADA',
  ARCHIVADA = 'ARCHIVADA',
}
